#include <stdlib.h>
#include <time.h>
#include<stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	signed char x;
	int n;

	for (n = 0; n <= 10; n++)
	{
	printf("%d", n);
	if (n == 10)
	{
	printf("\n");
	}

	}
	return (0);
}
